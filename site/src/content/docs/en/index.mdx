---
title: leo-cli
description: Personal command-line tools led by an interactive SQL IN workflow.
---

import VhsDemo from '../../../components/VhsDemo.astro';

`leo-cli` is a set of command-line tools for personal development workflows,
led by SQL IN construction and supported by repository navigation, time
conversion, image copying, and local log inspection.

Build from source:

```bash
make build
```

Quick start:

```bash
leo join
```

<VhsDemo
  src="demos/join.webp"
  caption="Choose a CSV field and output format, then copy the SQL IN list."
  tapeHref="https://github.com/leowzz/leo-cli/blob/main/site/vhs/join.tape"
  sourceLabel="View VHS source"
/>

Read [Getting Started](./getting-started/) for installation and your first SQL IN workflow,
follow the task-focused [Build SQL IN Values](./guides/join/), or browse the
[command reference](./reference/commands/leo/).
